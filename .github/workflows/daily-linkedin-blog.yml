name: Daily LinkedIn blog post

on:
  # DISABLED 2026-08-16: same cause as the Telegram and Facebook blog crons — it
  # links to ${BLOG_BASE_URL}, where 190 of the 224 posts 404. Picks the OLDEST
  # unposted post, so 185 of its 212 remaining picks are dead links.
  # Also disabled server-side via `gh workflow disable "Daily LinkedIn blog post"`.
  # To re-enable: uncomment the schedule below AND run
  #   gh workflow enable "Daily LinkedIn blog post"
  #
  # 13:47 UTC ~ 9:47 AM Eastern. 15 min after FB blog, 30 after Telegram blog.
  # schedule:
  #   - cron: '47 13 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-linkedin-blog
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - run: npm install --no-audit --no-fund

      - name: Send LinkedIn blog post
        env:
          LI_ACCESS_TOKEN: ${{ secrets.LI_ACCESS_TOKEN }}
          # Set ONE of these via repo Secrets:
          #   LI_PERSON_ID         - for personal-profile posting (Share on LinkedIn)
          #   LI_ORGANIZATION_ID   - for Company Page posting (Community Management API)
          # LI_AUTHOR_URN overrides both with a full URN string if you ever need
          # to specify the prefix yourself.
          LI_PERSON_ID: ${{ secrets.LI_PERSON_ID }}
          LI_ORGANIZATION_ID: ${{ secrets.LI_ORGANIZATION_ID }}
          LI_AUTHOR_URN: ${{ secrets.LI_AUTHOR_URN }}
          BLOG_BASE_URL: ${{ vars.BLOG_BASE_URL || 'https://nexitel-blog.vercel.app' }}
        run: npm run post:li-blog

      - name: Commit updated li-blog-log
        run: |
          if ! git diff --quiet -- scripts/li-blog-log.json; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add scripts/li-blog-log.json
            git commit -m "chore: record linkedin blog post for $(date -u +%Y-%m-%d)"
            git push
          else
            echo "li-blog-log.json unchanged; nothing to commit."
          fi
